<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".LoginHome"></activity>
        <activity android:name=".thankYou" />
        <activity android:name=".welcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TeamsConditions" />
        <activity android:name=".news_csr2" />
        <activity android:name=".ContactInfo" />
        <activity android:name=".news_csr" />
        <activity android:name=".finance_calculation" />
        <activity android:name=".CalcResult" />
        <activity android:name=".ArrangeResult" />
        <activity android:name=".ArrangeFinance" />
        <activity android:name=".interface01" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>